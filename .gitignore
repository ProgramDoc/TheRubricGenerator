# Runtime data (created by the app at startup — never commit these)
papers/
uploads/
obsidian_vault/
rubricgen.db
*.db

# OS / editor scratch
.DS_Store
.Rhistory

# Python
__pycache__/
*.py[cod]
.env
venv/
.venv/

# Claude Code (local tooling state — agent worktrees, local settings; never commit)
.claude/
